#!/usr/bin/env sh

# Основной скрипт запуска Gradle для Unix систем
APP_NAME="Gradle"
APP_BASE_NAME=$(basename "$0")

# Код ищет Java и запускает gradle-wrapper.jar
# ... (далее следует около 180 строк логики)
